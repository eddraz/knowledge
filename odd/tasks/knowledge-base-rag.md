# Feature: knowledge-base-rag

RAG knowledge base in Rust: SQLite + sqlite-vec (vector KNN, cosine) + FTS5 (lexical),
embeddings via bge-m3-Q8_0.gguf through a llama-server sidecar, grounded answers
generated by LFM2.5-230M-F16.gguf sidecar. CLI interface (`knowledge add|ask|search|list|rm`).

## Locked decisions (2026-09-20)

- Storage: sqlite-vec `vec0` virtual table (f32[1024], cosine) — NOT corpus-in-RAM brute force.
- Lexical: FTS5 `unicode61 remove_diacritics=2`. LIKE only for exact matches.
- Embeddings: bge-m3-Q8_0.gguf via upstream `llama-server --embedding` sidecar (verified: dim 1024, L2-normalized).
- Generator: LFM2.5-230M-F16.gguf via `llama-server` sidecar (K2-Horizon rejected for RAG: reasoning length too slow on target CPU).
- Interface: CLI (clap). Sidecars spawned/supervised by the app itself.
- Grounding contract: score gate (below threshold -> "not in knowledge base") + prompt restricted to context only.
- Chunking: ~400-800 chars, ~15% overlap, sentence/paragraph boundaries, configurable.
- Same model indexes and queries: bge-m3 only. Never mix embedding spaces.

## Tasks

### [x] 1. Scaffold project
cargo init, Cargo.toml deps (rusqlite bundled+load_extension, sqlite-vec, clap, tokio,
reqwest json, serde, anyhow, unicode-segmentation), src/config.rs (paths/ports/thresholds
with env overrides), src/error.rs. Feature branch off master.
- [x] commit: chore scaffold (evidence: b0be59c — folded into foundation commit)

### [x] 2. DB layer + sqlite-vec integration
DONE (delegated to gentle-ai-worker). rusqlite 0.37 auto_extension API used with
RawAutoExtension transmute of sqlite3_vec_init. vec0 KNN verified: distance is EUCLIDEAN
(orthogonal ~1.4142, opposite ~2.0) -> score = 1 - d^2/2. FTS5 accents tested (cafe==café).
Tests: vec_version, schema user_version, knn_roundtrip, fts accents, delete cascade.
- [x] commit: feat db layer (evidence: b0be59c)

### [x] 3. Embedding client + sidecar manager
DONE (same delegation). Pure parsers unit-tested (strict count/dim/finite validation);
live sidecar path deliberately deferred to E2E (task 7). Drop kills only self-spawned children.
- [x] commit: feat embeddings + sidecars (evidence: b0be59c)

NOTE: all three tasks landed as one work-unit commit b0be59c (14 tests green, verified by parent).

### [ ] 4. Ingest pipeline
Chunker: sentence/paragraph-aware, target 400-800 chars, 15% overlap, unit tests with
Spanish text. add flow: read file/stdin -> dedupe by hash -> chunk -> batch embed ->
insert chunks + fts + vec in one transaction. `knowledge add` works against live bge-m3.
- [ ] commit: feat ingest (evidence: <hash>)

### [ ] 5. Search modes
search.rs: vector KNN (vec0 MATCH, score = 1 - distance), lexical (FTS5 MATCH BM25),
optional RRF fusion. `knowledge search "q" [--mode] [-k]` prints chunks + scores.
- [ ] commit: feat search (evidence: <hash>)

### [ ] 6. Grounded ask
ask.rs: embed query -> KNN top-k -> score gate (< min_score -> "no está en la base")
-> context prompt (strict: answer ONLY from context, Spanish) -> LFM2.5 -> print answer
with source citations. `knowledge ask "pregunta" [-k]`.
- [ ] commit: feat ask (evidence: <hash>)

### [ ] 7. CLI wiring + end-to-end smoke test
clap subcommands add/ask/search/list/rm/status. E2E: add a real Spanish text file,
search semantically (paraphrase, no shared words), ask grounded question, verify refusal
on unrelated question. Supervisor kills spawned sidecars on exit.
- [ ] commit: feat cli + e2e (evidence: <hash>)

### [ ] 8. README + close
README: install, model paths config, usage examples, architecture diagram.
- [ ] commit: docs readme (evidence: <hash>)

## Notes
- Models live at ~/models/ (bge-m3-Q8_0.gguf, LFM2.5-230M-F16.gguf). llama-server at ~/.local/bin.
- Ports: embeddings 8098, generator 8099 (env-overridable).
- vec0 distance_metric=cosine if supported by installed sqlite-vec; else store L2-normalized
  vectors (server already normalizes) and convert: cosine = 1 - l2sq/2.
- Implementation delegated to gentle-ai-worker per multi-file write rule; parent orchestrates.
